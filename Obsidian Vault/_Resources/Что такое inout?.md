Используется когда мы хотим передать тип значение по ссылке, например в функцию

```Swift
func foo(a: inout Int) {
	print(a + 10)
}

let newA = 10

foo(&newA) // сейчас передаем по ссылке тип значение 
```